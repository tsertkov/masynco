jobs:
  include:
    - language: node_js
      node_js:
        - 12
        - node
        - lts/*
      before_script:
        - npm run build
    - language: node_js
      node_js:
        - node
      script:
        - npm run test-browser
      addons:
        chrome: stable

